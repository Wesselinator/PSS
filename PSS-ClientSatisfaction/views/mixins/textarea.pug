include fheader

mixin textarea(formID, headerText, placeholder)
    +fheader(formID, attributes.id, headerText)
        textarea(name!=attributes.id, placeholder=placeholder, required)&attributes(attributes)